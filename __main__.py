import os; from dotenv import load_dotenv; load_dotenv(); import asyncio; import discord; from discord.ext import commands; bot = commands.Bot(command_prefix=">"); exec('@bot.event\nasync def on_ready():\n    print(f"Bot Ready as {bot.user} ({bot.user.id}) - Created by fxcilities#4728")'); exec('@bot.command()\nasync def hello(ctx):\n    await ctx.send("World")'); bot.run(os.getenv("token"))
